import { Pick, Copy, Code } from "@site/src/components";

# Test

1. 문서 파일 소개
2. Pick
3. Copy

<Code lang="bash">
curl -v -G GET "https://kapi.kakao.com/v2/api/calendar/public/events" \
    -H "Authorization: KakaoAK 123123123" \
    -d "channel_public_id=_xnrxjem" \
    -d "from=2022-12-01T00:00:00Z" \
    -d "to=2022-12-11T00:00:00Z" \
    -d "limit=3"
</Code>


```bash
curl -v -G GET "https://kapi.kakao.com/v2/api/calendar/public/events" \
    -H "Authorization: KakaoAK 123123123" \
    -d "channel_public_id=_xnrxjem" \
    -d "from=2022-12-01T00:00:00Z" \
    -d "to=2022-12-11T00:00:00Z" \
    -d "limit=3"
```




<!-- <Code lang="bash">
curl -v -G GET "https://www.google.com"
</Code> -->

<br/>

1. CodeBlock 빼내서 simple editor 장착
1. curlCommand 파싱 방법 찾기
1. 위 요청을 js fetch 요청으로 응답받아서 아래에 div로 출력하도록

---

---

---

## Pick {#pick}

### API List - MD {#pick-md}

<Pick path="k-md" name method url />

### API List - MDX {#pick-mdx}

<Pick path="k-mdx" name="bold" method url="code" />

## Copy {#copy}

### MD

<Copy path="k-mdx#request-code" />

### MDX

## Need to fix: 해결 시 주석 처리

### html 태그 안 ${abc} 형태 빌드 크래시

## 프로젝트 정리

- docdo: 본 프로젝트
  - docupet: 이전 일부 작업
  - docdo-ref: 도큐사우루스 문서 사이트 클론
